<% content_for :title, "記事を編集 | ReadTyping" %>

<div class="w-full py-4 max-w-2xl mx-auto">
  <div class="mb-4">
    <h1 class="text-2xl font-bold text-gray-100">記事を編集</h1>
    <p class="text-sm text-gray-400 mt-1">タイトル・本文・カテゴリ・タグを編集できます。</p>
  </div>

  <div class="bg-gray-900 rounded-xl border border-gray-800 shadow-sm p-6">
    <%= form_with url: article_path(@article), method: :patch do |f| %>

      <%# エラーメッセージ %>
      <% if @form.errors.any? %>
        <div class="mb-4 p-4 bg-red-950 border border-red-800 rounded-lg">
          <ul class="list-disc list-inside text-sm text-red-400 space-y-1">
            <% @form.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%# タイトル %>
      <div class="mb-4">
        <label class="block text-xs font-bold text-gray-400 mb-1">タイトル</label>
        <%= f.text_field :title, value: @form.title,
            class: "w-full border border-gray-700 bg-gray-800 text-gray-100 placeholder-gray-500 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500",
            placeholder: "タイトル（省略可）" %>
      </div>

      <%# カテゴリ %>
      <div class="mb-4">
        <label class="block text-xs font-bold text-gray-400 mb-1">カテゴリ</label>
        <%= f.select :category,
            Article::CATEGORIES.map { |value, label| [label, value] },
            { include_blank: "未設定", selected: @form.category },
            class: "w-full border border-gray-700 bg-gray-800 text-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500" %>
      </div>

      <%# タグ %>
      <div class="mb-4">
        <label class="block text-xs font-bold text-gray-400 mb-1">タグ</label>
        <%= f.text_field :tag_names, value: @form.tag_names,
            class: "w-full border border-gray-700 bg-gray-800 text-gray-100 placeholder-gray-500 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500",
            placeholder: "カンマ区切りで入力（例: Ruby, Rails）" %>
        <p class="text-xs text-gray-500 mt-1">複数のタグはカンマ（,）で区切ってください。空欄にするとタグが削除されます。</p>
      </div>

      <%# 本文 %>
      <div class="mb-4">
        <label class="block text-xs font-bold text-gray-400 mb-1">本文</label>
        <%= f.text_area :body, value: @form.body,
            rows: 12,
            class: "w-full border border-gray-700 bg-gray-800 text-gray-100 placeholder-gray-500 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-green-500 font-mono",
            placeholder: "本文を入力してください（50〜10,000文字）" %>
      </div>

      <%# ボタン %>
      <div class="flex items-center justify-end gap-3 mt-6 pt-4 border-t border-gray-800">
        <%= link_to "キャンセル", articles_path,
            class: "text-sm font-bold text-gray-400 hover:text-gray-200 px-4 py-2 rounded-lg hover:bg-gray-800 transition-colors" %>
        <%= f.submit "更新する",
            class: "bg-green-500 hover:bg-green-700 text-gray-950 text-sm font-bold px-6 py-2 rounded-lg transition-colors cursor-pointer" %>
      </div>
    <% end %>
  </div>
</div>
