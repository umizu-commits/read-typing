<%= link_to page, url, { remote: remote, rel: page.next? ? "next" : page.prev? ? "prev" : nil, class: "w-8 h-8 flex items-center justify-center rounded text-gray-400 hover:text-green-500 hover:bg-gray-800 text-xs font-medium" } %>
